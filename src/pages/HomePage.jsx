import React from 'react';
import Menu from '../component/Menu';

const HomePage = () => {
    return (
        <div>
            <Menu/>
            <h1>Please Select Any Weather App</h1>
        </div>
    );
};

export default HomePage;